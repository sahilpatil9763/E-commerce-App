import { PageNotFoundDirective } from './page-not-found.directive';

describe('PageNotFoundDirective', () => {
  it('should create an instance', () => {
    const directive = new PageNotFoundDirective();
    expect(directive).toBeTruthy();
  });
});
