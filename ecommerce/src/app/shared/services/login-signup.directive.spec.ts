import { LoginSignupDirective } from './login-signup.directive';

describe('LoginSignupDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginSignupDirective();
    expect(directive).toBeTruthy();
  });
});
